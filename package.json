{
  "name": "talent-orbit-ats",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "lint": "pnpm --filter gateway lint && pnpm --filter auth lint",
    "test": "pnpm --filter gateway test -- --runInBand && pnpm --filter auth test",
    "build": "pnpm --filter gateway build && pnpm --filter auth build"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "pnpm@10.24.0"
}
