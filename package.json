{
  "name": "talent-orbit-ats",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "lint": "pnpm --filter gateway lint",
    "test": "pnpm --filter gateway test -- --runInBand",
    "build": "pnpm --filter gateway build"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "pnpm@10.24.0"
}
